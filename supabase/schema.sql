-- Schema de reference pour recreer la base sur un nouveau projet Supabase.
-- Source: version locale du projet.
-- Important: ce fichier reexporte le script de creation existant du projet.
-- Si tu importes depuis le SQL Editor Supabase, colle plutot le contenu de
-- `database_setup.sql` directement, car l'instruction ci-dessous n'est pas
-- interpretee par le SQL Editor web.

-- Fichier source canonique:
--   /database_setup.sql

